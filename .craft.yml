changelog:
  policy: auto
preReleaseCommand: bash bin/bump-version.sh
# This repo publishes only a git tag + GitHub release (no build artifacts uploaded by
# CI). Without this, craft's publish fails with "Can't find any artifacts for revision".
artifactProvider:
  name: none
targets:
  - name: github
